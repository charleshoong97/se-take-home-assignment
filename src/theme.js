import { createTheme } from "@mui/material";

export const theme = createTheme({
  box: {
    active: "#7ed1f7",
    inactive: "#d0d8db",
  },
  actions: {
    primary: "#e6241e",
    secondary: "#d4198f",
  },
});
